# Tool name/version constants shared between the ToolRegistration seed
# (db/seed.py) and the actual MCP tool implementations below, so the
# registry and the server can never silently drift apart.

GENERATE_PRODUCT_CAMPAIGN_TOOL = "generate_product_campaign"
GENERATE_PRODUCT_CAMPAIGN_VERSION = "1.0.0"

GENERATE_BEST_POSTING_TIME_TOOL = "generate_best_posting_time"
GENERATE_BEST_POSTING_TIME_VERSION = "1.0.0"
