# MCP

MCP tool wrappers exposed via the MCP Gateway (IBM/mcp-context-forge, Phase 7). Tools wrap existing Application Services only — never raw SQL, arbitrary HTTP, filesystem, or credentials. See `15_MCP_AGENTS_AND_SECURITY.md`.
