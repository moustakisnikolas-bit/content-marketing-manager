# Adapters

Concrete and fake implementations, one subpackage per port. See `28_OSS_TECHNOLOGY_STACK.md` for the concrete providers behind each port (LiteLLM+OpenRouter, Replicate, SeaweedFS, Stripe, etc.).
