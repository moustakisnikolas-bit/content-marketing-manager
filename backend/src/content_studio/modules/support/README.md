# Support Module

Moderation, support cases, notifications.

**Status: deliberately dormant.** The Phase 7 agent catalog (`db/seed.py`'s
`_AGENTS`) registers a "Support Agent" entry so the 8-agent catalog is
complete, but no models, API, or logic exist here yet — same precedent as
`ModelVersion` staying dormant in the analytics module until it's
genuinely ready, not an oversight. Build this out when a real support/
moderation-escalation need is prioritized, not speculatively.
