# Shopify App (Phase 6)

Remix + Polaris app using Shopify's official toolchain. GraphQL Admin API, OAuth/token exchange, mandatory GDPR compliance webhooks. See `14_SHOPIFY_APP_ARCHITECTURE.md`.

Reminder: reconfigure the CLI's default Prisma session store from SQLite to PostgreSQL before staging — SQLite is forbidden by the core spec.
